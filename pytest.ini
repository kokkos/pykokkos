[pytest]
# Root pytest configuration
# Excludes build artifacts and dependencies from test collection
norecursedirs = _skbuild _deps build .git external .eggs dist *.egg-info
testpaths = tests base/kokkos/test
