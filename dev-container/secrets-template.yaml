# Secrets template file
# Template values (username=root, password=pykPassword, port=empty, ssh=empty) will not be used
# Only non-template values will be added as Docker secrets

container_name: pyk-test
username: "root"
password: "pykPassword"
port: ""
ssh: ""
